# Swing Toolkit dependencies
pandas>=2.0
numpy>=1.24
yfinance>=0.2.40          # default free data feed (swap for your brokerage API)
requests>=2.31            # EDGAR filings retrieval
anthropic>=0.40           # cite-or-N/A LLM layer (needs ANTHROPIC_API_KEY)
